// This file previously contained mock data.
// All application data is now managed in Firebase Firestore.
// You can add, edit, and remove Services and Users through the Admin interface.

// The empty exports are kept to prevent import errors in existing components,
// but they are no longer used.

export const users = [];
export const services = [];
export const vehicles = [];
export const transactions = [];
